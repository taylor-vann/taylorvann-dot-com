worker_processes auto;

events {
  worker_connections auto;
}

http {
  # include default mime.types
  include       ./mime.types;
  default_type  application/octet-stream;

  include ./conf.d/taylorvann.nginx.conf;
}