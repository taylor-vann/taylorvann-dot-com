// request client session

// remove session

// the point here is to add cookies to a response
// sign-in is just a text response
// we use a form to make a fetch request
// cookie is added to response
// fetch request will return a response with a cookie,
// hopefully the browser picks up the session cookie!

package sessionrequests

// import (
// 	"github.com/taylor-vann/weblog/toolbox/golang/jwtx"
// )

// request session function
// confirm user
// if confirmed
//   issue client session
// else return Validation errors

func RequestSession() {

}


// remove session function
// write cookie as expired
func RemoveSession() {
	// grab cookie
	// send remove cookie

	// set cookie to expired on writer
	// return request
}

