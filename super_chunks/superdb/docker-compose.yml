# brian taylor vann
#

version: "3"
services:
  webapi:
    container_name: superdb_webapi_3000
    build:
      context: ./webapi/
      dockerfile: dockerfile
    restart: always
    ports:
      - "3000:80"
    volumes:
      - ./config/:/usr/local/config/
      - ./webapi/:/root/go/src/webapi/
    command: "go run /root/go/src/webapi/main.go"
  cache:
    depends_on:
      - superdb_webapi_3000
    container_name: superdb_cache_6379
    build:
      context: ./cache/
      dockerfile: dockerfile
    restart: always
    volumes:
      - ./cache/data/:/data
