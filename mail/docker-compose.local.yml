# brian taylor vann
# taylorvann dot com

# Use this docker-compose to create an authentication development server

version: "3"
services:
  webapi:
    container_name: mail_webapi
    hostname: mail.taylorvann.com
    build:
      context: ./webapi/
      dockerfile: dockerfile.dev
    restart: always
    env_file:
      - ./environment-variables.dev.env
    ports:
      - "6025:25"
      - "6000:6000"
    volumes:
      - ./webapi/:/go/src/webapi/
    command: "go run /go/src/webapi/main.go"
